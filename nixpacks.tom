# nixpacks.toml
name = "license-server"
description = "License Server for Greed Tool"

[phases.setup]
nixPkgs = ["postgresql", "gcc", "openssl"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "gunicorn run:app --bind 0.0.0.0:$PORT"